# .clawdbot Registry

Internal swarm registry used by `scripts/swarm-tmux`.

- `runs/` stores immutable run artifacts (metadata + logs)
- `templates/` stores reusable command templates
- `docs/` stores operational docs

Use `scripts/swarm-tmux init` to bootstrap defaults.
